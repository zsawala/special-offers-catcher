export type OfferProps = {
    id: string;
    title: string;
    description?: string;
    price: number;
};
export type CreateOfferProps = {
    title: string;
    description?: string;
    price: number;
};
